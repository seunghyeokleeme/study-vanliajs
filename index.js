const a = 221;
const b = a - 5;

console.log(b, a);

/* let, const, var 은 각각 변수이다.
    let 은 변수가 변할수 있다.
    const 은 상수이다. (변할수 없는 고정값) 현재 이변수를 가장 많이 쓰인다.
    >> Why??  -- 오류를 쉽게 찾을 수있기 때문이다.
    var 은 3년전까지 이 변수만 쓰이게 되었다.
*/
